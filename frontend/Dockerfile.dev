FROM node:22-alpine

WORKDIR /app

# Copy package files
COPY package.json package-lock.json ./

# Install dependencies
RUN npm ci

# Copy all files (will be overridden by volume mounts for hot reload dirs)
COPY . .

# Expose port
EXPOSE 3000

# Run development server with hot reloading
CMD ["npm", "run", "dev"]
